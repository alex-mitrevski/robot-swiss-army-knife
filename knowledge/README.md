# knowledge

Components used for robot knowledge acquisition and management.

The following ROS 2 packages are included here:
* `vlm_query_interface`: A Python-based package that exposes an interface for sending VLMs / LLMs queries.